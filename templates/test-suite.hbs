<test-suite type="{{type}}" id="{{id}}" name="{{name}}" fullname="{{fullname}}" testcasecount="{{testCaseCount}}" result="{{result}}" time="{{time}}" total="{{total}}" passed="{{passed}}" failed="{{failed}}" inconclusive="{{inconclusive}}" skipped="{{skipped}}" asserts="{{asserts}}">
    {{#if properties }} {{> properties }} {{/if}}

    {{#each testCases}}
        {{> test-case }}
    {{/each}}

    {{#each testSuites}}
        {{> test-suite }}
    {{/each}}
</test-suite>
