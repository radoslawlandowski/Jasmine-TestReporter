<test-case 
    id="{{id}}" 
    name="{{name}}" 
    fullname="{{fullName}}" 
    result="{{result}}" 
    time="{{time}}" 
    asserts="{{asserts}}">

  {{#if properties }} {{> properties }} {{/if}}

</test-case>