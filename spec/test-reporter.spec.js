describe("Test reporter tests", function() {
    describe("Test reporter tests", function() {
        var a;
      
        it("and so is a spec", function() {
          a = true;
      
          expect(a).toBe(true);
        });
      });
  
    it("and so is a spec", function() {
      a = true;
  
      expect(a).toBe(true);
    });
  });